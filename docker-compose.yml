services:
  jekyll:
    build: .
    ports:
      - "4000:4000"
    command: bash -c "bundle install && bundle exec jekyll serve --watch --host '0.0.0.0' --config _config.yml,_config.dev.yml"
    volumes:
      - .:/srv/jekyll
      - ./bundle_cache:/usr/local/bundle
